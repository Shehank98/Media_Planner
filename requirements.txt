# Canonical dependency list. Kept at the repo root because Railpack copies only
# this file into the (cached) install layer, so it cannot rely on an include of
# backend/requirements.txt. backend/requirements.txt re-exports this list.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
openpyxl==3.1.5
python-dateutil==2.9.0.post0
matplotlib==3.10.0
reportlab==4.2.5
python-docx==1.1.2
google-genai==0.8.0
